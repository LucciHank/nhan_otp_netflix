<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title : 'Hệ Thống Nhận OTP Netflix' %></title>
  <link rel="icon" href="/img/favicon.ico" type="image/x-icon">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/base64-images.css">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            netflix: {
              red: '#E50914',
              dark: '#221F1F',
              light: '#F5F5F1'
            }
          }
        }
      }
    }
  </script>
  <style>
    body {
      background-color: #141414;
      background-image: radial-gradient(circle at center, #1F1F1F 0%, #141414 50%, #000000 100%);
      min-height: 100vh;
      color: #F5F5F1;
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    }
    
    .text-netflix-red {
      color: #E50914;
    }
    
    .text-netflix-light {
      color: #F5F5F1;
    }
    
    .bg-netflix-dark {
      background-color: #141414;
    }
    
    .bg-netflix-red {
      background-color: #E50914;
    }
    
    .main-container {
      min-height: calc(100vh - 150px);
      padding: 2rem;
      position: relative;
      overflow: hidden;
    }
    
    .footer {
      height: 70px;
      background-color: rgba(0, 0, 0, 0.7);
      color: #757575;
      font-size: 0.8rem;
      border-top: 1px solid rgba(255, 255, 255, 0.05);
    }
    
    /* App logo styling */
    .app-logo {
      font-size: 1.8rem;
      font-weight: bold;
      padding: 0.5rem 1rem;
      color: #E50914;
      text-shadow: 0 0 10px rgba(229, 9, 20, 0.3);
    }
    
    .header {
      background-color: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(255, 255, 255, 0.08);
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
      position: relative;
      z-index: 100;
    }
    
    .card {
      background-color: rgba(20, 20, 20, 0.7);
      backdrop-filter: blur(10px);
      border-radius: 8px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.5);
      overflow: hidden;
      border: 1px solid rgba(229, 9, 20, 0.1);
    }
    
    .card-header {
      background-color: rgba(0, 0, 0, 0.4);
      padding: 1rem;
      border-bottom: 1px solid rgba(229, 9, 20, 0.2);
    }
    
    .card-body {
      padding: 1.5rem;
    }
    
    .mascot-image {
      display: none;
    }
  </style>
  <%- typeof extractStyles !== 'undefined' ? extractStyles : '' %>
</head>
<body class="bg-netflix-dark text-netflix-light">
  <header class="header shadow-md py-3">
    <div class="container mx-auto flex justify-between items-center px-4">
      <div class="app-logo">
        <a href="/" class="flex items-center">
          <img src="/img/tomoi-logo.svg" alt="TomOi.vn" class="h-10 mr-2">
        </a>
      </div>
      <% if (typeof req !== 'undefined' && req.session && req.session.user) { %>
        <div class="flex items-center space-x-4">
          <a href="/admin/dashboard" class="text-white hover:text-gray-300">Dashboard</a>
          <a href="/admin/accounts" class="text-white hover:text-gray-300">Tài khoản</a>
          <a href="/admin/tools" class="text-white hover:text-gray-300">Tools</a>
          <span class="text-gray-400">|</span>
          <a href="/admin/logout" class="text-white hover:text-gray-300">Đăng xuất</a>
    </div>
      <% } %>
    </div>
  </header>

  <main class="main-container container mx-auto">
        <%- body %>
  </main>
  
  <footer class="footer mt-8 py-5">
    <div class="container mx-auto px-4 text-center">
      <p>&copy; <%= new Date().getFullYear() %> TomOi.vn - Hệ thống nhận OTP Netflix hộ gia đình</p>
    </div>
  </footer>
  
  <%- typeof extractScripts !== 'undefined' ? extractScripts : '' %>
</body>
</html>
